# Directory holding the Wikipedia/Wiktionary dump files
# (also used by download_data.sh and mounted into the container).
DUMP_PATH=/dump

# Optional overrides (defaults shown):
# PORT=9095
# WEB_DIR=web/dist
